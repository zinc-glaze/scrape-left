<div class="container">
  <div class="row">
    <div class="col s12">
      {{#each articles}}
        <div class="card">
          <div class="card-content">
            <span class="card-title">
              {{title}}
            </span>
            <p>
              {{summary}}
            </p>

          </div>
          <div class="card-action">
            <a class="waves-effect waves-light btn save-article">button</a>
          </div>
        </div>
      {{/each}}
    </div>
  </div>
</div>